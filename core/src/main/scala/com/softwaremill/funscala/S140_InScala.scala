package com.softwaremill.funscala

object S140_InScala {
  /*
  Scala macros added in 2.10.0 as an experimental feature
  
  def assert(cond: Boolean, msg: Any) = macro Asserts.assertImpl
  
  object Asserts {
    def assertImpl(c: Context)(cond: c.Expr[Boolean], msg: c.Expr[Any]) : c.Expr[Unit] = ???
  }
   */
  
  // Later added: implicit macros, macro annotations
}
